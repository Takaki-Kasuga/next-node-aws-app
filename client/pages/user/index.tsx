import React, { FC } from 'react';

// components
import { Header } from '../../components/layout/index';

const UserDashboard: FC = () => {
  return <Header>UserDashboard</Header>;
};

export default UserDashboard;
